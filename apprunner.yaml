# https://docs.aws.amazon.com/apprunner/latest/dg/service-source-code-nodejs.html
version: 1.0
runtime: nodejs16
build:
  commands:
    - npm install
run:
  command: npm start
network:
  port: 3000
  env: APP_PORT
